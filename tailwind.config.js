/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      './**/*.html', // This will include all HTML files in all subdirectories
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }